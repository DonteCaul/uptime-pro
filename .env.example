# PostgreSQL
POSTGRES_DB=uptime
POSTGRES_USER=uptime
POSTGRES_PASSWORD=changeme

# Backend
JWT_SECRET=change-this-to-a-long-random-string
GOOGLE_PLACES_KEY=
DEKUNU_COMPAT=false

# Frontend (baked into build at deploy time)
VITE_MAPBOX_TOKEN=
VITE_API_URL=https://uptime.pro
